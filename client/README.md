# Bungash Designs - Client (React)

This is a Create-React-App style client. It contains a simple Home page and an Admin page that talks to the server API.

Quick start:

```bash
cd client
npm install
npm start
```

By default the app calls `/api/*` on the same origin. To change the API URL, set `REACT_APP_API_URL` in environment.
